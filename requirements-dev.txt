# FlagBase Backend — development & test dependencies

pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
aiosqlite==0.20.0   # SQLite async driver for in-process tests (no Postgres needed)

black==24.10.0
ruff==0.7.1
mypy==1.13.0
